# given an integer N, write a program which read N inputs and prints them

N = int(input("Enter the number of input to read : "))

for i in range(N):
    inputs = input("Enter the input to read : ")
    print(inputs)
    